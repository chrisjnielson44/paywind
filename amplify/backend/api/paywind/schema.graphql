type UserInfo @model @auth(rules: [{allow: public}]) {
  id: ID!
  firstname: String
  lastname: String
  email: String
  phonenumber: String
  age: Int
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}
 